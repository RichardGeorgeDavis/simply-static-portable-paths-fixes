# Changelog

## 2026-02-04
- Added `simply-static-fixes` tool with UI (`index.php`) and rewrite engine (`fix.php`).
- Added `sites/` folder for drop-in static exports.
- Added `add-this/simply-static-portable-paths.php` for WordPress plugin installation.
- Added agent notes (`AGENTS.md`) and usage docs (`README.md`).
